note
 description:"[
		SimpleBilly API
 		Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
  		The version of the OpenAPI document: 0.1.0
 	    Contact: support@simplebilly.com

  	NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).

 		 Do not edit the class manually.
 	]"
	date: "$Date$"
	revision: "$Revision$"
	EIS:"Eiffel openapi generator", "src=https://openapi-generator.tech", "protocol=uri"
class JOB




feature --Access

    attempts: INTEGER_32
      
    job_type: detachable STRING_32
      -- Discriminator the worker dispatches on (e.g. \"webhook.deliver\").
    max_attempts: INTEGER_32
      
    payload: detachable ANY
      
    run_at: detachable DATE_TIME
      -- Earliest execution time; None = run now.
    status: detachable JOB_STATUS
      -- pending | running | done | failed

feature -- Change Element

    set_attempts (a_name: like attempts)
        -- Set 'attempts' with 'a_name'.
      do
        attempts := a_name
      ensure
        attempts_set: attempts = a_name
      end

    set_job_type (a_name: like job_type)
        -- Set 'job_type' with 'a_name'.
      do
        job_type := a_name
      ensure
        job_type_set: job_type = a_name
      end

    set_max_attempts (a_name: like max_attempts)
        -- Set 'max_attempts' with 'a_name'.
      do
        max_attempts := a_name
      ensure
        max_attempts_set: max_attempts = a_name
      end

    set_payload (a_name: like payload)
        -- Set 'payload' with 'a_name'.
      do
        payload := a_name
      ensure
        payload_set: payload = a_name
      end

    set_run_at (a_name: like run_at)
        -- Set 'run_at' with 'a_name'.
      do
        run_at := a_name
      ensure
        run_at_set: run_at = a_name
      end

    set_status (a_name: like status)
        -- Set 'status' with 'a_name'.
      do
        status := a_name
      ensure
        status_set: status = a_name
      end


 feature -- Status Report

    output: STRING
          -- <Precursor>
      do
        create Result.make_empty
        Result.append("%Nclass JOB%N")
        if attached attempts as l_attempts then
          Result.append ("%Nattempts:")
          Result.append (l_attempts.out)
          Result.append ("%N")
        end
        if attached job_type as l_job_type then
          Result.append ("%Njob_type:")
          Result.append (l_job_type.out)
          Result.append ("%N")
        end
        if attached max_attempts as l_max_attempts then
          Result.append ("%Nmax_attempts:")
          Result.append (l_max_attempts.out)
          Result.append ("%N")
        end
        if attached payload as l_payload then
          Result.append ("%Npayload:")
          Result.append (l_payload.out)
          Result.append ("%N")
        end
        if attached run_at as l_run_at then
          Result.append ("%Nrun_at:")
          Result.append (l_run_at.out)
          Result.append ("%N")
        end
        if attached status as l_status then
          Result.append ("%Nstatus:")
          Result.append (l_status.out)
          Result.append ("%N")
        end
      end
end

