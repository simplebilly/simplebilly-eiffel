note
 description:"[
		SimpleBilly API
 		Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
  		The version of the OpenAPI document: 0.1.0
 	    Contact: support@simplebilly.com

  	NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).

 		 Do not edit the class manually.
 	]"
	date: "$Date$"
	revision: "$Revision$"
	EIS:"Eiffel openapi generator", "src=https://openapi-generator.tech", "protocol=uri"
class GENERATE_VARIANTS_REQUEST




feature --Access

    options: detachable STRING_TABLE [LIST [STRING_32]]
      -- Option name → list of values, e.g. `{\"Color\": [\"Red\", \"Blue\"], \"Size\": [\"S\", \"M\"]}`. The cartesian product of these lists is generated.
    price_delta: detachable STRING_32
      -- Optional per-variant price delta applied to every generated variant.
    product_id: detachable UUID
      
    sku_prefix: detachable STRING_32
      -- Optional prefix for the generated SKUs (suffix is the option values joined by `-`). Falls back to the parent product's SKU.

feature -- Change Element

    set_options (a_name: like options)
        -- Set 'options' with 'a_name'.
      do
        options := a_name
      ensure
        options_set: options = a_name
      end

    set_price_delta (a_name: like price_delta)
        -- Set 'price_delta' with 'a_name'.
      do
        price_delta := a_name
      ensure
        price_delta_set: price_delta = a_name
      end

    set_product_id (a_name: like product_id)
        -- Set 'product_id' with 'a_name'.
      do
        product_id := a_name
      ensure
        product_id_set: product_id = a_name
      end

    set_sku_prefix (a_name: like sku_prefix)
        -- Set 'sku_prefix' with 'a_name'.
      do
        sku_prefix := a_name
      ensure
        sku_prefix_set: sku_prefix = a_name
      end


 feature -- Status Report

    output: STRING
          -- <Precursor>
      do
        create Result.make_empty
        Result.append("%Nclass GENERATE_VARIANTS_REQUEST%N")
        if attached options as l_options then
          Result.append ("%Noptions:")
          across l_options as ic loop
            Result.append ("%N")
            Result.append ("key:")
            Result.append (ic.key.out)
            Result.append (" - ")
            Result.append ("val:")
            Result.append (ic.item.out)
            Result.append ("%N")
          end
        end
        if attached price_delta as l_price_delta then
          Result.append ("%Nprice_delta:")
          Result.append (l_price_delta.out)
          Result.append ("%N")
        end
        if attached product_id as l_product_id then
          Result.append ("%Nproduct_id:")
          Result.append (l_product_id.out)
          Result.append ("%N")
        end
        if attached sku_prefix as l_sku_prefix then
          Result.append ("%Nsku_prefix:")
          Result.append (l_sku_prefix.out)
          Result.append ("%N")
        end
      end
end

