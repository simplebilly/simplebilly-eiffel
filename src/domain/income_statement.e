note
 description:"[
		SimpleBilly API
 		Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
  		The version of the OpenAPI document: 0.1.0
 	    Contact: support@simplebilly.com

  	NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).

 		 Do not edit the class manually.
 	]"
	date: "$Date$"
	revision: "$Revision$"
	EIS:"Eiffel openapi generator", "src=https://openapi-generator.tech", "protocol=uri"
class INCOME_STATEMENT




feature --Access

    expense_items: detachable LIST [PN_L_ITEM]
      
    net_income: detachable STRING_32
      
    revenue_items: detachable LIST [PN_L_ITEM]
      
    total_expenses: detachable STRING_32
      
    total_revenue: detachable STRING_32
      

feature -- Change Element

    set_expense_items (a_name: like expense_items)
        -- Set 'expense_items' with 'a_name'.
      do
        expense_items := a_name
      ensure
        expense_items_set: expense_items = a_name
      end

    set_net_income (a_name: like net_income)
        -- Set 'net_income' with 'a_name'.
      do
        net_income := a_name
      ensure
        net_income_set: net_income = a_name
      end

    set_revenue_items (a_name: like revenue_items)
        -- Set 'revenue_items' with 'a_name'.
      do
        revenue_items := a_name
      ensure
        revenue_items_set: revenue_items = a_name
      end

    set_total_expenses (a_name: like total_expenses)
        -- Set 'total_expenses' with 'a_name'.
      do
        total_expenses := a_name
      ensure
        total_expenses_set: total_expenses = a_name
      end

    set_total_revenue (a_name: like total_revenue)
        -- Set 'total_revenue' with 'a_name'.
      do
        total_revenue := a_name
      ensure
        total_revenue_set: total_revenue = a_name
      end


 feature -- Status Report

    output: STRING
          -- <Precursor>
      do
        create Result.make_empty
        Result.append("%Nclass INCOME_STATEMENT%N")
        if attached expense_items as l_expense_items then
          across l_expense_items as ic loop
            Result.append ("%N expense_items:")
            Result.append (ic.item.out)
            Result.append ("%N")
          end
        end
        if attached net_income as l_net_income then
          Result.append ("%Nnet_income:")
          Result.append (l_net_income.out)
          Result.append ("%N")
        end
        if attached revenue_items as l_revenue_items then
          across l_revenue_items as ic loop
            Result.append ("%N revenue_items:")
            Result.append (ic.item.out)
            Result.append ("%N")
          end
        end
        if attached total_expenses as l_total_expenses then
          Result.append ("%Ntotal_expenses:")
          Result.append (l_total_expenses.out)
          Result.append ("%N")
        end
        if attached total_revenue as l_total_revenue then
          Result.append ("%Ntotal_revenue:")
          Result.append (l_total_revenue.out)
          Result.append ("%N")
        end
      end
end

