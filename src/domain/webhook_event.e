note
 description:"[
		SimpleBilly API
 		Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
  		The version of the OpenAPI document: 0.1.0
 	    Contact: support@simplebilly.com

  	NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).

 		 Do not edit the class manually.
 	]"
	date: "$Date$"
	revision: "$Revision$"
	EIS:"Eiffel openapi generator", "src=https://openapi-generator.tech", "protocol=uri"
class WEBHOOK_EVENT




feature --Access

    attempts: INTEGER_32
      
    channel: detachable STRING_32
      -- source for inbound, target URL for outbound.
    direction: detachable WEBHOOK_DIRECTION
      -- inbound | outbound
    event_type: detachable STRING_32
      
    last_error: detachable STRING_32
      
    payload: detachable ANY
      
    status: detachable WEBHOOK_EVENT_STATUS
      -- accepted | delivered | failed

feature -- Change Element

    set_attempts (a_name: like attempts)
        -- Set 'attempts' with 'a_name'.
      do
        attempts := a_name
      ensure
        attempts_set: attempts = a_name
      end

    set_channel (a_name: like channel)
        -- Set 'channel' with 'a_name'.
      do
        channel := a_name
      ensure
        channel_set: channel = a_name
      end

    set_direction (a_name: like direction)
        -- Set 'direction' with 'a_name'.
      do
        direction := a_name
      ensure
        direction_set: direction = a_name
      end

    set_event_type (a_name: like event_type)
        -- Set 'event_type' with 'a_name'.
      do
        event_type := a_name
      ensure
        event_type_set: event_type = a_name
      end

    set_last_error (a_name: like last_error)
        -- Set 'last_error' with 'a_name'.
      do
        last_error := a_name
      ensure
        last_error_set: last_error = a_name
      end

    set_payload (a_name: like payload)
        -- Set 'payload' with 'a_name'.
      do
        payload := a_name
      ensure
        payload_set: payload = a_name
      end

    set_status (a_name: like status)
        -- Set 'status' with 'a_name'.
      do
        status := a_name
      ensure
        status_set: status = a_name
      end


 feature -- Status Report

    output: STRING
          -- <Precursor>
      do
        create Result.make_empty
        Result.append("%Nclass WEBHOOK_EVENT%N")
        if attached attempts as l_attempts then
          Result.append ("%Nattempts:")
          Result.append (l_attempts.out)
          Result.append ("%N")
        end
        if attached channel as l_channel then
          Result.append ("%Nchannel:")
          Result.append (l_channel.out)
          Result.append ("%N")
        end
        if attached direction as l_direction then
          Result.append ("%Ndirection:")
          Result.append (l_direction.out)
          Result.append ("%N")
        end
        if attached event_type as l_event_type then
          Result.append ("%Nevent_type:")
          Result.append (l_event_type.out)
          Result.append ("%N")
        end
        if attached last_error as l_last_error then
          Result.append ("%Nlast_error:")
          Result.append (l_last_error.out)
          Result.append ("%N")
        end
        if attached payload as l_payload then
          Result.append ("%Npayload:")
          Result.append (l_payload.out)
          Result.append ("%N")
        end
        if attached status as l_status then
          Result.append ("%Nstatus:")
          Result.append (l_status.out)
          Result.append ("%N")
        end
      end
end

