note
 description:"[
		SimpleBilly API
 		Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
  		The version of the OpenAPI document: 0.1.0
 	    Contact: support@simplebilly.com

  	NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).

 		 Do not edit the class manually.
 	]"
	date: "$Date$"
	revision: "$Revision$"
	EIS:"Eiffel openapi generator", "src=https://openapi-generator.tech", "protocol=uri"
class BOM_CREATE




feature --Access

    components: detachable ANY
      -- JSON array of `{product_id, name, quantity, unit, scrap_rate}`.
    description: detachable STRING_32
      
    name: detachable STRING_32
      
    output_quantity: INTEGER_64
      -- Output quantity per production run (defaults to 1).
    product_id: detachable UUID
      -- The finished product this BOM produces. References the product entity.
    status: detachable BOM_STATUS
      -- One of: draft | active | archived

feature -- Change Element

    set_components (a_name: like components)
        -- Set 'components' with 'a_name'.
      do
        components := a_name
      ensure
        components_set: components = a_name
      end

    set_description (a_name: like description)
        -- Set 'description' with 'a_name'.
      do
        description := a_name
      ensure
        description_set: description = a_name
      end

    set_name (a_name: like name)
        -- Set 'name' with 'a_name'.
      do
        name := a_name
      ensure
        name_set: name = a_name
      end

    set_output_quantity (a_name: like output_quantity)
        -- Set 'output_quantity' with 'a_name'.
      do
        output_quantity := a_name
      ensure
        output_quantity_set: output_quantity = a_name
      end

    set_product_id (a_name: like product_id)
        -- Set 'product_id' with 'a_name'.
      do
        product_id := a_name
      ensure
        product_id_set: product_id = a_name
      end

    set_status (a_name: like status)
        -- Set 'status' with 'a_name'.
      do
        status := a_name
      ensure
        status_set: status = a_name
      end


 feature -- Status Report

    output: STRING
          -- <Precursor>
      do
        create Result.make_empty
        Result.append("%Nclass BOM_CREATE%N")
        if attached components as l_components then
          Result.append ("%Ncomponents:")
          Result.append (l_components.out)
          Result.append ("%N")
        end
        if attached description as l_description then
          Result.append ("%Ndescription:")
          Result.append (l_description.out)
          Result.append ("%N")
        end
        if attached name as l_name then
          Result.append ("%Nname:")
          Result.append (l_name.out)
          Result.append ("%N")
        end
        if attached output_quantity as l_output_quantity then
          Result.append ("%Noutput_quantity:")
          Result.append (l_output_quantity.out)
          Result.append ("%N")
        end
        if attached product_id as l_product_id then
          Result.append ("%Nproduct_id:")
          Result.append (l_product_id.out)
          Result.append ("%N")
        end
        if attached status as l_status then
          Result.append ("%Nstatus:")
          Result.append (l_status.out)
          Result.append ("%N")
        end
      end
end

