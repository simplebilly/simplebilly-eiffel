note
 description:"[
		SimpleBilly API
 		Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
  		The version of the OpenAPI document: 0.1.0
 	    Contact: support@simplebilly.com

  	NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).

 		 Do not edit the class manually.
 	]"
	date: "$Date$"
	revision: "$Revision$"
	EIS:"Eiffel openapi generator", "src=https://openapi-generator.tech", "protocol=uri"
class SUBMIT_RESULT_DTO




feature --Access

    answers: detachable LIST [INTEGER_32]
      -- Selected answer indices (required for scored builtin trainings).
    assignment_id: detachable UUID
      
    score: INTEGER_32
      -- Score 0–100. Only trusted for plugin trainings without server-side scoring; builtin trainings are always re-scored from `answers`.
    training_code: detachable STRING_32
      

feature -- Change Element

    set_answers (a_name: like answers)
        -- Set 'answers' with 'a_name'.
      do
        answers := a_name
      ensure
        answers_set: answers = a_name
      end

    set_assignment_id (a_name: like assignment_id)
        -- Set 'assignment_id' with 'a_name'.
      do
        assignment_id := a_name
      ensure
        assignment_id_set: assignment_id = a_name
      end

    set_score (a_name: like score)
        -- Set 'score' with 'a_name'.
      do
        score := a_name
      ensure
        score_set: score = a_name
      end

    set_training_code (a_name: like training_code)
        -- Set 'training_code' with 'a_name'.
      do
        training_code := a_name
      ensure
        training_code_set: training_code = a_name
      end


 feature -- Status Report

    output: STRING
          -- <Precursor>
      do
        create Result.make_empty
        Result.append("%Nclass SUBMIT_RESULT_DTO%N")
        if attached answers as l_answers then
          across l_answers as ic loop
            Result.append ("%N answers:")
            Result.append (ic.item.out)
            Result.append ("%N")
          end
        end
        if attached assignment_id as l_assignment_id then
          Result.append ("%Nassignment_id:")
          Result.append (l_assignment_id.out)
          Result.append ("%N")
        end
        if attached score as l_score then
          Result.append ("%Nscore:")
          Result.append (l_score.out)
          Result.append ("%N")
        end
        if attached training_code as l_training_code then
          Result.append ("%Ntraining_code:")
          Result.append (l_training_code.out)
          Result.append ("%N")
        end
      end
end

