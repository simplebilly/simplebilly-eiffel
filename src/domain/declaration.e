note
 description:"[
		SimpleBilly API
 		Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
  		The version of the OpenAPI document: 0.1.0
 	    Contact: support@simplebilly.com

  	NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).

 		 Do not edit the class manually.
 	]"
	date: "$Date$"
	revision: "$Revision$"
	EIS:"Eiffel openapi generator", "src=https://openapi-generator.tech", "protocol=uri"
class DECLARATION




feature --Access

    declaration_type: detachable DECLARATION_TYPE
      -- Art der Erklärung: \"dcgk\" (Entsprechenserklärung § 161 AktG) oder \"unternehmensfuehrung\" (Erklärung zur Unternehmensführung § 289f HGB).
    is_current: BOOLEAN
      -- Kennzeichnet die aktuell gültige Fassung (max. eine je Mandant).
    text: detachable STRING_32
      -- Inhalt der Erklärung als Markdown.
    valid_from: detachable DATE
      -- Datum, ab dem die Erklärung gilt.
    version: detachable STRING_32
      -- Versionsbezeichnung der Erklärung (z.B. \"2025-01\").

feature -- Change Element

    set_declaration_type (a_name: like declaration_type)
        -- Set 'declaration_type' with 'a_name'.
      do
        declaration_type := a_name
      ensure
        declaration_type_set: declaration_type = a_name
      end

    set_is_current (a_name: like is_current)
        -- Set 'is_current' with 'a_name'.
      do
        is_current := a_name
      ensure
        is_current_set: is_current = a_name
      end

    set_text (a_name: like text)
        -- Set 'text' with 'a_name'.
      do
        text := a_name
      ensure
        text_set: text = a_name
      end

    set_valid_from (a_name: like valid_from)
        -- Set 'valid_from' with 'a_name'.
      do
        valid_from := a_name
      ensure
        valid_from_set: valid_from = a_name
      end

    set_version (a_name: like version)
        -- Set 'version' with 'a_name'.
      do
        version := a_name
      ensure
        version_set: version = a_name
      end


 feature -- Status Report

    output: STRING
          -- <Precursor>
      do
        create Result.make_empty
        Result.append("%Nclass DECLARATION%N")
        if attached declaration_type as l_declaration_type then
          Result.append ("%Ndeclaration_type:")
          Result.append (l_declaration_type.out)
          Result.append ("%N")
        end
        if attached is_current as l_is_current then
          Result.append ("%Nis_current:")
          Result.append (l_is_current.out)
          Result.append ("%N")
        end
        if attached text as l_text then
          Result.append ("%Ntext:")
          Result.append (l_text.out)
          Result.append ("%N")
        end
        if attached valid_from as l_valid_from then
          Result.append ("%Nvalid_from:")
          Result.append (l_valid_from.out)
          Result.append ("%N")
        end
        if attached version as l_version then
          Result.append ("%Nversion:")
          Result.append (l_version.out)
          Result.append ("%N")
        end
      end
end

