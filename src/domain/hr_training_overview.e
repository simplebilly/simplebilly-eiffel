note
 description:"[
		SimpleBilly API
 		Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
  		The version of the OpenAPI document: 0.1.0
 	    Contact: support@simplebilly.com

  	NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).

 		 Do not edit the class manually.
 	]"
	date: "$Date$"
	revision: "$Revision$"
	EIS:"Eiffel openapi generator", "src=https://openapi-generator.tech", "protocol=uri"
class HR_TRAINING_OVERVIEW




feature --Access

    assigned_count: INTEGER_64
      
    code: detachable STRING_32
      
    completed_count: INTEGER_64
      
    overdue_count: INTEGER_64
      
    title: detachable STRING_32
      
    training_id: detachable UUID
      

feature -- Change Element

    set_assigned_count (a_name: like assigned_count)
        -- Set 'assigned_count' with 'a_name'.
      do
        assigned_count := a_name
      ensure
        assigned_count_set: assigned_count = a_name
      end

    set_code (a_name: like code)
        -- Set 'code' with 'a_name'.
      do
        code := a_name
      ensure
        code_set: code = a_name
      end

    set_completed_count (a_name: like completed_count)
        -- Set 'completed_count' with 'a_name'.
      do
        completed_count := a_name
      ensure
        completed_count_set: completed_count = a_name
      end

    set_overdue_count (a_name: like overdue_count)
        -- Set 'overdue_count' with 'a_name'.
      do
        overdue_count := a_name
      ensure
        overdue_count_set: overdue_count = a_name
      end

    set_title (a_name: like title)
        -- Set 'title' with 'a_name'.
      do
        title := a_name
      ensure
        title_set: title = a_name
      end

    set_training_id (a_name: like training_id)
        -- Set 'training_id' with 'a_name'.
      do
        training_id := a_name
      ensure
        training_id_set: training_id = a_name
      end


 feature -- Status Report

    output: STRING
          -- <Precursor>
      do
        create Result.make_empty
        Result.append("%Nclass HR_TRAINING_OVERVIEW%N")
        if attached assigned_count as l_assigned_count then
          Result.append ("%Nassigned_count:")
          Result.append (l_assigned_count.out)
          Result.append ("%N")
        end
        if attached code as l_code then
          Result.append ("%Ncode:")
          Result.append (l_code.out)
          Result.append ("%N")
        end
        if attached completed_count as l_completed_count then
          Result.append ("%Ncompleted_count:")
          Result.append (l_completed_count.out)
          Result.append ("%N")
        end
        if attached overdue_count as l_overdue_count then
          Result.append ("%Noverdue_count:")
          Result.append (l_overdue_count.out)
          Result.append ("%N")
        end
        if attached title as l_title then
          Result.append ("%Ntitle:")
          Result.append (l_title.out)
          Result.append ("%N")
        end
        if attached training_id as l_training_id then
          Result.append ("%Ntraining_id:")
          Result.append (l_training_id.out)
          Result.append ("%N")
        end
      end
end

