note
 description:"[
		SimpleBilly API
 		Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
  		The version of the OpenAPI document: 0.1.0
 	    Contact: support@simplebilly.com

  	NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).

 		 Do not edit the class manually.
 	]"
	date: "$Date$"
	revision: "$Revision$"
	EIS:"Eiffel openapi generator", "src=https://openapi-generator.tech", "protocol=uri"
class PRODUCT_ATTRIBUTE_CREATE




feature --Access

    is_filterable: BOOLEAN
      -- Whether this attribute participates in the shop's faceted filters.
    name: detachable STRING_32
      -- Attribute name, e.g. `Material`, `Farbe`, `Gewicht`.
    position: INTEGER_32
      -- Ordering position within the product's attribute list.
    product_id: detachable UUID
      -- The product this attribute belongs to. References the product entity.
    unit: detachable STRING_32
      -- Optional unit of measure for numeric attributes, e.g. `g`, `cm`.
    value: detachable STRING_32
      -- Attribute value, e.g. `Baumwolle`, `Rot`, `180g`.

feature -- Change Element

    set_is_filterable (a_name: like is_filterable)
        -- Set 'is_filterable' with 'a_name'.
      do
        is_filterable := a_name
      ensure
        is_filterable_set: is_filterable = a_name
      end

    set_name (a_name: like name)
        -- Set 'name' with 'a_name'.
      do
        name := a_name
      ensure
        name_set: name = a_name
      end

    set_position (a_name: like position)
        -- Set 'position' with 'a_name'.
      do
        position := a_name
      ensure
        position_set: position = a_name
      end

    set_product_id (a_name: like product_id)
        -- Set 'product_id' with 'a_name'.
      do
        product_id := a_name
      ensure
        product_id_set: product_id = a_name
      end

    set_unit (a_name: like unit)
        -- Set 'unit' with 'a_name'.
      do
        unit := a_name
      ensure
        unit_set: unit = a_name
      end

    set_value (a_name: like value)
        -- Set 'value' with 'a_name'.
      do
        value := a_name
      ensure
        value_set: value = a_name
      end


 feature -- Status Report

    output: STRING
          -- <Precursor>
      do
        create Result.make_empty
        Result.append("%Nclass PRODUCT_ATTRIBUTE_CREATE%N")
        if attached is_filterable as l_is_filterable then
          Result.append ("%Nis_filterable:")
          Result.append (l_is_filterable.out)
          Result.append ("%N")
        end
        if attached name as l_name then
          Result.append ("%Nname:")
          Result.append (l_name.out)
          Result.append ("%N")
        end
        if attached position as l_position then
          Result.append ("%Nposition:")
          Result.append (l_position.out)
          Result.append ("%N")
        end
        if attached product_id as l_product_id then
          Result.append ("%Nproduct_id:")
          Result.append (l_product_id.out)
          Result.append ("%N")
        end
        if attached unit as l_unit then
          Result.append ("%Nunit:")
          Result.append (l_unit.out)
          Result.append ("%N")
        end
        if attached value as l_value then
          Result.append ("%Nvalue:")
          Result.append (l_value.out)
          Result.append ("%N")
        end
      end
end

