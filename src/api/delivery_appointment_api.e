note
 description:"[
		SimpleBilly API
 		Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
  		The version of the OpenAPI document: 0.1.0
 	    Contact: support@simplebilly.com

  	NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).

 		 Do not edit the class manually.
 	]"
	date: "$Date$"
	revision: "$Revision$"
	EIS:"Eiffel openapi generator", "src=https://openapi-generator.tech", "protocol=uri"

class
	DELIVERYAPPOINTMENT_API

inherit

    API_I


feature -- API Access


	create_delivery_appointment (delivery_appointment_create: DELIVERY_APPOINTMENT_CREATE): detachable DELIVERY_APPOINTMENT
			-- 
			-- 
			-- 
			-- argument: delivery_appointment_create  (required)
			-- 
			-- 
			-- Result DELIVERY_APPOINTMENT
		require
		local
  			l_path: STRING
  			l_request: API_CLIENT_REQUEST
  			l_response: API_CLIENT_RESPONSE
		do
			reset_error
			create l_request
			l_request.set_body(delivery_appointment_create)
			l_path := "/api/v1/delivery-appointments"


			if attached {STRING} api_client.select_header_accept ({ARRAY [STRING]}<<"application/json">>)  as l_accept then
				l_request.add_header(l_accept,"Accept");
			end
			l_request.add_header(api_client.select_header_content_type ({ARRAY [STRING]}<<"application/json">>),"Content-Type")
			l_request.set_auth_names ({ARRAY [STRING]}<<"bearer_token">>)
			l_response := api_client.call_api (l_path, "Post", l_request, Void, agent deserializer)
			if l_response.has_error then
				last_error := l_response.error
			elseif attached { DELIVERY_APPOINTMENT } l_response.data ({ DELIVERY_APPOINTMENT }) as l_data then
				Result := l_data
			else
				create last_error.make ("Unknown error: Status response [ " + l_response.status.out + "]")
			end
		end

	delete_delivery_appointment (appointment_id: STRING_32)
			-- 
			-- 
			-- 
			-- argument: appointment_id  (required)
			-- 
			-- 
		require
		local
  			l_path: STRING
  			l_request: API_CLIENT_REQUEST
  			l_response: API_CLIENT_RESPONSE
		do
			reset_error
			create l_request
			
			l_path := "/api/v1/delivery-appointments/{appointment_id}"
			l_path.replace_substring_all ("{"+"appointment_id"+"}", api_client.url_encode (appointment_id.out))


			if attached {STRING} api_client.select_header_accept ({ARRAY [STRING]}<<"application/json">>)  as l_accept then
				l_request.add_header(l_accept,"Accept");
			end
			l_request.add_header(api_client.select_header_content_type ({ARRAY [STRING]}<<>>),"Content-Type")
			l_request.set_auth_names ({ARRAY [STRING]}<<"bearer_token">>)
			l_response := api_client.call_api (l_path, "Delete", l_request, agent serializer, Void)
			if l_response.has_error then
				last_error := l_response.error
			end
		end

	delivery_appointment (appointment_id: STRING_32): detachable DELIVERY_APPOINTMENT
			-- 
			-- 
			-- 
			-- argument: appointment_id  (required)
			-- 
			-- 
			-- Result DELIVERY_APPOINTMENT
		require
		local
  			l_path: STRING
  			l_request: API_CLIENT_REQUEST
  			l_response: API_CLIENT_RESPONSE
		do
			reset_error
			create l_request
			
			l_path := "/api/v1/delivery-appointments/{appointment_id}"
			l_path.replace_substring_all ("{"+"appointment_id"+"}", api_client.url_encode (appointment_id.out))


			if attached {STRING} api_client.select_header_accept ({ARRAY [STRING]}<<"application/json">>)  as l_accept then
				l_request.add_header(l_accept,"Accept");
			end
			l_request.add_header(api_client.select_header_content_type ({ARRAY [STRING]}<<>>),"Content-Type")
			l_request.set_auth_names ({ARRAY [STRING]}<<"bearer_token">>)
			l_response := api_client.call_api (l_path, "Get", l_request, Void, agent deserializer)
			if l_response.has_error then
				last_error := l_response.error
			elseif attached { DELIVERY_APPOINTMENT } l_response.data ({ DELIVERY_APPOINTMENT }) as l_data then
				Result := l_data
			else
				create last_error.make ("Unknown error: Status response [ " + l_response.status.out + "]")
			end
		end

	list_delivery_appointments (page: INTEGER_32; page_size: INTEGER_32; status: STRING_32; warehouse_id: STRING_32; var_from: DATE; to: DATE): detachable LIST [DELIVERY_APPOINTMENT]
			-- 
			-- 
			-- 
			-- argument: page  (optional, default to null)
			-- 
			-- argument: page_size  (optional, default to null)
			-- 
			-- argument: status  (optional, default to null)
			-- 
			-- argument: warehouse_id  (optional, default to null)
			-- 
			-- argument: var_from  (optional, default to null)
			-- 
			-- argument: to  (optional, default to null)
			-- 
			-- 
			-- Result LIST [DELIVERY_APPOINTMENT]
		require
     		page_is_greater_or_equal_than: page >= 0
     		page_size_is_greater_or_equal_than: page_size >= 0
		local
  			l_path: STRING
  			l_request: API_CLIENT_REQUEST
  			l_response: API_CLIENT_RESPONSE
		do
			reset_error
			create l_request
			
			l_path := "/api/v1/delivery-appointments"
			l_request.fill_query_params(api_client.parameter_to_tuple("", "page", page));
			l_request.fill_query_params(api_client.parameter_to_tuple("", "page_size", page_size));
			l_request.fill_query_params(api_client.parameter_to_tuple("", "status", status));
			l_request.fill_query_params(api_client.parameter_to_tuple("", "warehouse_id", warehouse_id));
			l_request.fill_query_params(api_client.parameter_to_tuple("", "from", var_from));
			l_request.fill_query_params(api_client.parameter_to_tuple("", "to", to));


			if attached {STRING} api_client.select_header_accept ({ARRAY [STRING]}<<"application/json">>)  as l_accept then
				l_request.add_header(l_accept,"Accept");
			end
			l_request.add_header(api_client.select_header_content_type ({ARRAY [STRING]}<<>>),"Content-Type")
			l_request.set_auth_names ({ARRAY [STRING]}<<"bearer_token">>)
			l_response := api_client.call_api (l_path, "Get", l_request, Void, agent deserializer)
			if l_response.has_error then
				last_error := l_response.error
			elseif attached { LIST [DELIVERY_APPOINTMENT] } l_response.data ({ LIST [DELIVERY_APPOINTMENT] }) as l_data then
				Result := l_data
			else
				create last_error.make ("Unknown error: Status response [ " + l_response.status.out + "]")
			end
		end

	public_delivery_appointment_status (appointment_id: STRING_32; email: STRING_32; token: STRING_32): detachable PUBLIC_DELIVERY_APPOINTMENT_STATUS_RESPONSE
			-- Supplier/carrier checks appointment status (public, no auth). The appointment is only revealed when email AND token match.
			-- 
			-- 
			-- argument: appointment_id  (required)
			-- 
			-- argument: email  (required)
			-- 
			-- argument: token  (required)
			-- 
			-- 
			-- Result PUBLIC_DELIVERY_APPOINTMENT_STATUS_RESPONSE
		require
		local
  			l_path: STRING
  			l_request: API_CLIENT_REQUEST
  			l_response: API_CLIENT_RESPONSE
		do
			reset_error
			create l_request
			
			l_path := "/api/v1/public/delivery-appointments/status"
			l_request.fill_query_params(api_client.parameter_to_tuple("", "appointmentId", appointment_id));
			l_request.fill_query_params(api_client.parameter_to_tuple("", "email", email));
			l_request.fill_query_params(api_client.parameter_to_tuple("", "token", token));


			if attached {STRING} api_client.select_header_accept ({ARRAY [STRING]}<<"application/json">>)  as l_accept then
				l_request.add_header(l_accept,"Accept");
			end
			l_request.add_header(api_client.select_header_content_type ({ARRAY [STRING]}<<>>),"Content-Type")
			l_request.set_auth_names ({ARRAY [STRING]}<<"bearer_token">>)
			l_response := api_client.call_api (l_path, "Get", l_request, Void, agent deserializer)
			if l_response.has_error then
				last_error := l_response.error
			elseif attached { PUBLIC_DELIVERY_APPOINTMENT_STATUS_RESPONSE } l_response.data ({ PUBLIC_DELIVERY_APPOINTMENT_STATUS_RESPONSE }) as l_data then
				Result := l_data
			else
				create last_error.make ("Unknown error: Status response [ " + l_response.status.out + "]")
			end
		end

	request_public_delivery_appointment (public_delivery_appointment_request: PUBLIC_DELIVERY_APPOINTMENT_REQUEST): detachable PUBLIC_DELIVERY_APPOINTMENT_RESPONSE
			-- Supplier/carrier requests an inbound delivery slot (public, no auth). The tenant is derived from the warehouse found by &#x60;code&#x60; — never from the request.
			-- 
			-- 
			-- argument: public_delivery_appointment_request  (required)
			-- 
			-- 
			-- Result PUBLIC_DELIVERY_APPOINTMENT_RESPONSE
		require
		local
  			l_path: STRING
  			l_request: API_CLIENT_REQUEST
  			l_response: API_CLIENT_RESPONSE
		do
			reset_error
			create l_request
			l_request.set_body(public_delivery_appointment_request)
			l_path := "/api/v1/public/delivery-appointments/request"


			if attached {STRING} api_client.select_header_accept ({ARRAY [STRING]}<<"application/json">>)  as l_accept then
				l_request.add_header(l_accept,"Accept");
			end
			l_request.add_header(api_client.select_header_content_type ({ARRAY [STRING]}<<"application/json">>),"Content-Type")
			l_request.set_auth_names ({ARRAY [STRING]}<<"bearer_token">>)
			l_response := api_client.call_api (l_path, "Post", l_request, Void, agent deserializer)
			if l_response.has_error then
				last_error := l_response.error
			elseif attached { PUBLIC_DELIVERY_APPOINTMENT_RESPONSE } l_response.data ({ PUBLIC_DELIVERY_APPOINTMENT_RESPONSE }) as l_data then
				Result := l_data
			else
				create last_error.make ("Unknown error: Status response [ " + l_response.status.out + "]")
			end
		end

	update_delivery_appointment (appointment_id: STRING_32; body: ANY): detachable DELIVERY_APPOINTMENT
			-- 
			-- 
			-- 
			-- argument: appointment_id  (required)
			-- 
			-- argument: body  (required)
			-- 
			-- 
			-- Result DELIVERY_APPOINTMENT
		require
		local
  			l_path: STRING
  			l_request: API_CLIENT_REQUEST
  			l_response: API_CLIENT_RESPONSE
		do
			reset_error
			create l_request
			l_request.set_body(body)
			l_path := "/api/v1/delivery-appointments/{appointment_id}"
			l_path.replace_substring_all ("{"+"appointment_id"+"}", api_client.url_encode (appointment_id.out))


			if attached {STRING} api_client.select_header_accept ({ARRAY [STRING]}<<"application/json">>)  as l_accept then
				l_request.add_header(l_accept,"Accept");
			end
			l_request.add_header(api_client.select_header_content_type ({ARRAY [STRING]}<<"application/json">>),"Content-Type")
			l_request.set_auth_names ({ARRAY [STRING]}<<"bearer_token">>)
			l_response := api_client.call_api (l_path, "Put", l_request, Void, agent deserializer)
			if l_response.has_error then
				last_error := l_response.error
			elseif attached { DELIVERY_APPOINTMENT } l_response.data ({ DELIVERY_APPOINTMENT }) as l_data then
				Result := l_data
			else
				create last_error.make ("Unknown error: Status response [ " + l_response.status.out + "]")
			end
		end

	update_delivery_appointment_status (appointment_id: STRING_32; appointment_status_update: APPOINTMENT_STATUS_UPDATE): detachable DELIVERY_APPOINTMENT
			-- 
			-- 
			-- 
			-- argument: appointment_id  (required)
			-- 
			-- argument: appointment_status_update  (required)
			-- 
			-- 
			-- Result DELIVERY_APPOINTMENT
		require
		local
  			l_path: STRING
  			l_request: API_CLIENT_REQUEST
  			l_response: API_CLIENT_RESPONSE
		do
			reset_error
			create l_request
			l_request.set_body(appointment_status_update)
			l_path := "/api/v1/delivery-appointments/{appointment_id}/status"
			l_path.replace_substring_all ("{"+"appointment_id"+"}", api_client.url_encode (appointment_id.out))


			if attached {STRING} api_client.select_header_accept ({ARRAY [STRING]}<<"application/json">>)  as l_accept then
				l_request.add_header(l_accept,"Accept");
			end
			l_request.add_header(api_client.select_header_content_type ({ARRAY [STRING]}<<"application/json">>),"Content-Type")
			l_request.set_auth_names ({ARRAY [STRING]}<<"bearer_token">>)
			l_response := api_client.call_api (l_path, "Put", l_request, Void, agent deserializer)
			if l_response.has_error then
				last_error := l_response.error
			elseif attached { DELIVERY_APPOINTMENT } l_response.data ({ DELIVERY_APPOINTMENT }) as l_data then
				Result := l_data
			else
				create last_error.make ("Unknown error: Status response [ " + l_response.status.out + "]")
			end
		end


end
