note
 description:"[
		SimpleBilly API
 		Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
  		The version of the OpenAPI document: 0.1.0
 	    Contact: support@simplebilly.com

  	NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).

 		 Do not edit the class manually.
 	]"
	date: "$Date$"
	revision: "$Revision$"
	EIS:"Eiffel openapi generator", "src=https://openapi-generator.tech", "protocol=uri"

class
	SHIPPINGTHRESHOLD_API

inherit

    API_I


feature -- API Access


	create_shipping_threshold (shipping_threshold_create: SHIPPING_THRESHOLD_CREATE): detachable SHIPPING_THRESHOLD
			-- 
			-- 
			-- 
			-- argument: shipping_threshold_create  (required)
			-- 
			-- 
			-- Result SHIPPING_THRESHOLD
		require
		local
  			l_path: STRING
  			l_request: API_CLIENT_REQUEST
  			l_response: API_CLIENT_RESPONSE
		do
			reset_error
			create l_request
			l_request.set_body(shipping_threshold_create)
			l_path := "/api/v1/shipping-thresholds"


			if attached {STRING} api_client.select_header_accept ({ARRAY [STRING]}<<"application/json">>)  as l_accept then
				l_request.add_header(l_accept,"Accept");
			end
			l_request.add_header(api_client.select_header_content_type ({ARRAY [STRING]}<<"application/json">>),"Content-Type")
			l_request.set_auth_names ({ARRAY [STRING]}<<"bearer_token">>)
			l_response := api_client.call_api (l_path, "Post", l_request, Void, agent deserializer)
			if l_response.has_error then
				last_error := l_response.error
			elseif attached { SHIPPING_THRESHOLD } l_response.data ({ SHIPPING_THRESHOLD }) as l_data then
				Result := l_data
			else
				create last_error.make ("Unknown error: Status response [ " + l_response.status.out + "]")
			end
		end

	delete_shipping_threshold (threshold_id: STRING_32)
			-- 
			-- 
			-- 
			-- argument: threshold_id  (required)
			-- 
			-- 
		require
		local
  			l_path: STRING
  			l_request: API_CLIENT_REQUEST
  			l_response: API_CLIENT_RESPONSE
		do
			reset_error
			create l_request
			
			l_path := "/api/v1/shipping-thresholds/{threshold_id}"
			l_path.replace_substring_all ("{"+"threshold_id"+"}", api_client.url_encode (threshold_id.out))


			if attached {STRING} api_client.select_header_accept ({ARRAY [STRING]}<<"application/json">>)  as l_accept then
				l_request.add_header(l_accept,"Accept");
			end
			l_request.add_header(api_client.select_header_content_type ({ARRAY [STRING]}<<>>),"Content-Type")
			l_request.set_auth_names ({ARRAY [STRING]}<<"bearer_token">>)
			l_response := api_client.call_api (l_path, "Delete", l_request, agent serializer, Void)
			if l_response.has_error then
				last_error := l_response.error
			end
		end

	deliverable (product_id: UUID; warehouse_id: STRING_32): detachable DELIVERABLE_RESPONSE
			-- 
			-- 
			-- 
			-- argument: product_id  (required)
			-- 
			-- argument: warehouse_id  (optional, default to null)
			-- 
			-- 
			-- Result DELIVERABLE_RESPONSE
		require
		local
  			l_path: STRING
  			l_request: API_CLIENT_REQUEST
  			l_response: API_CLIENT_RESPONSE
		do
			reset_error
			create l_request
			
			l_path := "/api/v1/shipping-thresholds/deliverable"
			l_request.fill_query_params(api_client.parameter_to_tuple("", "productId", product_id));
			l_request.fill_query_params(api_client.parameter_to_tuple("", "warehouseId", warehouse_id));


			if attached {STRING} api_client.select_header_accept ({ARRAY [STRING]}<<"application/json">>)  as l_accept then
				l_request.add_header(l_accept,"Accept");
			end
			l_request.add_header(api_client.select_header_content_type ({ARRAY [STRING]}<<>>),"Content-Type")
			l_request.set_auth_names ({ARRAY [STRING]}<<"bearer_token">>)
			l_response := api_client.call_api (l_path, "Get", l_request, Void, agent deserializer)
			if l_response.has_error then
				last_error := l_response.error
			elseif attached { DELIVERABLE_RESPONSE } l_response.data ({ DELIVERABLE_RESPONSE }) as l_data then
				Result := l_data
			else
				create last_error.make ("Unknown error: Status response [ " + l_response.status.out + "]")
			end
		end

	list_shipping_thresholds (page: INTEGER_32; page_size: INTEGER_32; product_id: UUID; warehouse_id: STRING_32; is_active: BOOLEAN): detachable LIST [SHIPPING_THRESHOLD]
			-- 
			-- 
			-- 
			-- argument: page  (optional, default to null)
			-- 
			-- argument: page_size  (optional, default to null)
			-- 
			-- argument: product_id  (optional, default to null)
			-- 
			-- argument: warehouse_id  (optional, default to null)
			-- 
			-- argument: is_active  (optional, default to null)
			-- 
			-- 
			-- Result LIST [SHIPPING_THRESHOLD]
		require
     		page_is_greater_or_equal_than: page >= 0
     		page_size_is_greater_or_equal_than: page_size >= 0
		local
  			l_path: STRING
  			l_request: API_CLIENT_REQUEST
  			l_response: API_CLIENT_RESPONSE
		do
			reset_error
			create l_request
			
			l_path := "/api/v1/shipping-thresholds/"
			l_request.fill_query_params(api_client.parameter_to_tuple("", "page", page));
			l_request.fill_query_params(api_client.parameter_to_tuple("", "page_size", page_size));
			l_request.fill_query_params(api_client.parameter_to_tuple("", "product_id", product_id));
			l_request.fill_query_params(api_client.parameter_to_tuple("", "warehouse_id", warehouse_id));
			l_request.fill_query_params(api_client.parameter_to_tuple("", "is_active", is_active));


			if attached {STRING} api_client.select_header_accept ({ARRAY [STRING]}<<"application/json">>)  as l_accept then
				l_request.add_header(l_accept,"Accept");
			end
			l_request.add_header(api_client.select_header_content_type ({ARRAY [STRING]}<<>>),"Content-Type")
			l_request.set_auth_names ({ARRAY [STRING]}<<"bearer_token">>)
			l_response := api_client.call_api (l_path, "Get", l_request, Void, agent deserializer)
			if l_response.has_error then
				last_error := l_response.error
			elseif attached { LIST [SHIPPING_THRESHOLD] } l_response.data ({ LIST [SHIPPING_THRESHOLD] }) as l_data then
				Result := l_data
			else
				create last_error.make ("Unknown error: Status response [ " + l_response.status.out + "]")
			end
		end

	shipping_threshold (threshold_id: STRING_32): detachable SHIPPING_THRESHOLD
			-- 
			-- 
			-- 
			-- argument: threshold_id  (required)
			-- 
			-- 
			-- Result SHIPPING_THRESHOLD
		require
		local
  			l_path: STRING
  			l_request: API_CLIENT_REQUEST
  			l_response: API_CLIENT_RESPONSE
		do
			reset_error
			create l_request
			
			l_path := "/api/v1/shipping-thresholds/{threshold_id}"
			l_path.replace_substring_all ("{"+"threshold_id"+"}", api_client.url_encode (threshold_id.out))


			if attached {STRING} api_client.select_header_accept ({ARRAY [STRING]}<<"application/json">>)  as l_accept then
				l_request.add_header(l_accept,"Accept");
			end
			l_request.add_header(api_client.select_header_content_type ({ARRAY [STRING]}<<>>),"Content-Type")
			l_request.set_auth_names ({ARRAY [STRING]}<<"bearer_token">>)
			l_response := api_client.call_api (l_path, "Get", l_request, Void, agent deserializer)
			if l_response.has_error then
				last_error := l_response.error
			elseif attached { SHIPPING_THRESHOLD } l_response.data ({ SHIPPING_THRESHOLD }) as l_data then
				Result := l_data
			else
				create last_error.make ("Unknown error: Status response [ " + l_response.status.out + "]")
			end
		end

	update_shipping_threshold (threshold_id: STRING_32; shipping_threshold_update: SHIPPING_THRESHOLD_UPDATE): detachable SHIPPING_THRESHOLD
			-- 
			-- 
			-- 
			-- argument: threshold_id  (required)
			-- 
			-- argument: shipping_threshold_update  (required)
			-- 
			-- 
			-- Result SHIPPING_THRESHOLD
		require
		local
  			l_path: STRING
  			l_request: API_CLIENT_REQUEST
  			l_response: API_CLIENT_RESPONSE
		do
			reset_error
			create l_request
			l_request.set_body(shipping_threshold_update)
			l_path := "/api/v1/shipping-thresholds/{threshold_id}"
			l_path.replace_substring_all ("{"+"threshold_id"+"}", api_client.url_encode (threshold_id.out))


			if attached {STRING} api_client.select_header_accept ({ARRAY [STRING]}<<"application/json">>)  as l_accept then
				l_request.add_header(l_accept,"Accept");
			end
			l_request.add_header(api_client.select_header_content_type ({ARRAY [STRING]}<<"application/json">>),"Content-Type")
			l_request.set_auth_names ({ARRAY [STRING]}<<"bearer_token">>)
			l_response := api_client.call_api (l_path, "Put", l_request, Void, agent deserializer)
			if l_response.has_error then
				last_error := l_response.error
			elseif attached { SHIPPING_THRESHOLD } l_response.data ({ SHIPPING_THRESHOLD }) as l_data then
				Result := l_data
			else
				create last_error.make ("Unknown error: Status response [ " + l_response.status.out + "]")
			end
		end


end
