note
 description:"[
		SimpleBilly API
 		Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
  		The version of the OpenAPI document: 0.1.0
 	    Contact: support@simplebilly.com

  	NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).

 		 Do not edit the class manually.
 	]"
	date: "$Date$"
	revision: "$Revision$"
	EIS:"Eiffel openapi generator", "src=https://openapi-generator.tech", "protocol=uri"

class
	PUBLICRETURNS_API

inherit

    API_I


feature -- API Access


	list_public_returns (order_number: STRING_32; email: STRING_32): detachable LIST [PUBLIC_RETURN_STATUS_RESPONSE]
			-- List all returns for an order (public, no auth).
			-- 
			-- 
			-- argument: order_number  (required)
			-- 
			-- argument: email  (required)
			-- 
			-- 
			-- Result LIST [PUBLIC_RETURN_STATUS_RESPONSE]
		require
		local
  			l_path: STRING
  			l_request: API_CLIENT_REQUEST
  			l_response: API_CLIENT_RESPONSE
		do
			reset_error
			create l_request
			
			l_path := "/api/v1/public/returns/list"
			l_request.fill_query_params(api_client.parameter_to_tuple("", "orderNumber", order_number));
			l_request.fill_query_params(api_client.parameter_to_tuple("", "email", email));


			if attached {STRING} api_client.select_header_accept ({ARRAY [STRING]}<<"application/json">>)  as l_accept then
				l_request.add_header(l_accept,"Accept");
			end
			l_request.add_header(api_client.select_header_content_type ({ARRAY [STRING]}<<>>),"Content-Type")
			l_request.set_auth_names ({ARRAY [STRING]}<<"bearer_token">>)
			l_response := api_client.call_api (l_path, "Get", l_request, Void, agent deserializer)
			if l_response.has_error then
				last_error := l_response.error
			elseif attached { LIST [PUBLIC_RETURN_STATUS_RESPONSE] } l_response.data ({ LIST [PUBLIC_RETURN_STATUS_RESPONSE] }) as l_data then
				Result := l_data
			else
				create last_error.make ("Unknown error: Status response [ " + l_response.status.out + "]")
			end
		end

	public_return_status (email: STRING_32; return_number: STRING_32; return_order_id: STRING_32; order_number: STRING_32): detachable PUBLIC_RETURN_STATUS_RESPONSE
			-- Customer checks the status of a return (public, no auth). The return is only revealed when its linked order&#39;s email matches.
			-- 
			-- 
			-- argument: email  (required)
			-- 
			-- argument: return_number Either return_number or return_order_id must be provided. (optional, default to null)
			-- 
			-- argument: return_order_id  (optional, default to null)
			-- 
			-- argument: order_number  (optional, default to null)
			-- 
			-- 
			-- Result PUBLIC_RETURN_STATUS_RESPONSE
		require
		local
  			l_path: STRING
  			l_request: API_CLIENT_REQUEST
  			l_response: API_CLIENT_RESPONSE
		do
			reset_error
			create l_request
			
			l_path := "/api/v1/public/returns/status"
			l_request.fill_query_params(api_client.parameter_to_tuple("", "returnNumber", return_number));
			l_request.fill_query_params(api_client.parameter_to_tuple("", "returnOrderId", return_order_id));
			l_request.fill_query_params(api_client.parameter_to_tuple("", "email", email));
			l_request.fill_query_params(api_client.parameter_to_tuple("", "orderNumber", order_number));


			if attached {STRING} api_client.select_header_accept ({ARRAY [STRING]}<<"application/json">>)  as l_accept then
				l_request.add_header(l_accept,"Accept");
			end
			l_request.add_header(api_client.select_header_content_type ({ARRAY [STRING]}<<>>),"Content-Type")
			l_request.set_auth_names ({ARRAY [STRING]}<<"bearer_token">>)
			l_response := api_client.call_api (l_path, "Get", l_request, Void, agent deserializer)
			if l_response.has_error then
				last_error := l_response.error
			elseif attached { PUBLIC_RETURN_STATUS_RESPONSE } l_response.data ({ PUBLIC_RETURN_STATUS_RESPONSE }) as l_data then
				Result := l_data
			else
				create last_error.make ("Unknown error: Status response [ " + l_response.status.out + "]")
			end
		end

	request_public_return (public_return_request: PUBLIC_RETURN_REQUEST): detachable PUBLIC_RETURN_RESPONSE
			-- Customer requests a return for an order (public, no auth).
			-- 
			-- 
			-- argument: public_return_request  (required)
			-- 
			-- 
			-- Result PUBLIC_RETURN_RESPONSE
		require
		local
  			l_path: STRING
  			l_request: API_CLIENT_REQUEST
  			l_response: API_CLIENT_RESPONSE
		do
			reset_error
			create l_request
			l_request.set_body(public_return_request)
			l_path := "/api/v1/public/returns/request"


			if attached {STRING} api_client.select_header_accept ({ARRAY [STRING]}<<"application/json">>)  as l_accept then
				l_request.add_header(l_accept,"Accept");
			end
			l_request.add_header(api_client.select_header_content_type ({ARRAY [STRING]}<<"application/json">>),"Content-Type")
			l_request.set_auth_names ({ARRAY [STRING]}<<"bearer_token">>)
			l_response := api_client.call_api (l_path, "Post", l_request, Void, agent deserializer)
			if l_response.has_error then
				last_error := l_response.error
			elseif attached { PUBLIC_RETURN_RESPONSE } l_response.data ({ PUBLIC_RETURN_RESPONSE }) as l_data then
				Result := l_data
			else
				create last_error.make ("Unknown error: Status response [ " + l_response.status.out + "]")
			end
		end


end
