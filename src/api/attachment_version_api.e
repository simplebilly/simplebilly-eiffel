note
 description:"[
		SimpleBilly API
 		Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
  		The version of the OpenAPI document: 0.1.0
 	    Contact: support@simplebilly.com

  	NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).

 		 Do not edit the class manually.
 	]"
	date: "$Date$"
	revision: "$Revision$"
	EIS:"Eiffel openapi generator", "src=https://openapi-generator.tech", "protocol=uri"

class
	ATTACHMENTVERSION_API

inherit

    API_I


feature -- API Access


	create_attachment_version (attachment_id: UUID; new_version_request: NEW_VERSION_REQUEST): detachable ATTACHMENT_VERSION
			-- 
			-- 
			-- 
			-- argument: attachment_id  (required)
			-- 
			-- argument: new_version_request  (required)
			-- 
			-- 
			-- Result ATTACHMENT_VERSION
		require
		local
  			l_path: STRING
  			l_request: API_CLIENT_REQUEST
  			l_response: API_CLIENT_RESPONSE
		do
			reset_error
			create l_request
			l_request.set_body(new_version_request)
			l_path := "/api/v1/attachments/{attachment_id}/versions"
			l_path.replace_substring_all ("{"+"attachment_id"+"}", api_client.url_encode (attachment_id.out))


			if attached {STRING} api_client.select_header_accept ({ARRAY [STRING]}<<"application/json">>)  as l_accept then
				l_request.add_header(l_accept,"Accept");
			end
			l_request.add_header(api_client.select_header_content_type ({ARRAY [STRING]}<<"application/json">>),"Content-Type")
			l_request.set_auth_names ({ARRAY [STRING]}<<"bearer_token">>)
			l_response := api_client.call_api (l_path, "Post", l_request, Void, agent deserializer)
			if l_response.has_error then
				last_error := l_response.error
			elseif attached { ATTACHMENT_VERSION } l_response.data ({ ATTACHMENT_VERSION }) as l_data then
				Result := l_data
			else
				create last_error.make ("Unknown error: Status response [ " + l_response.status.out + "]")
			end
		end

	list_attachment_versions (attachment_id: UUID): detachable LIST [ATTACHMENT_VERSION]
			-- 
			-- 
			-- 
			-- argument: attachment_id  (required)
			-- 
			-- 
			-- Result LIST [ATTACHMENT_VERSION]
		require
		local
  			l_path: STRING
  			l_request: API_CLIENT_REQUEST
  			l_response: API_CLIENT_RESPONSE
		do
			reset_error
			create l_request
			
			l_path := "/api/v1/attachments/{attachment_id}/versions"
			l_path.replace_substring_all ("{"+"attachment_id"+"}", api_client.url_encode (attachment_id.out))


			if attached {STRING} api_client.select_header_accept ({ARRAY [STRING]}<<"application/json">>)  as l_accept then
				l_request.add_header(l_accept,"Accept");
			end
			l_request.add_header(api_client.select_header_content_type ({ARRAY [STRING]}<<>>),"Content-Type")
			l_request.set_auth_names ({ARRAY [STRING]}<<"bearer_token">>)
			l_response := api_client.call_api (l_path, "Get", l_request, Void, agent deserializer)
			if l_response.has_error then
				last_error := l_response.error
			elseif attached { LIST [ATTACHMENT_VERSION] } l_response.data ({ LIST [ATTACHMENT_VERSION] }) as l_data then
				Result := l_data
			else
				create last_error.make ("Unknown error: Status response [ " + l_response.status.out + "]")
			end
		end

	restore_attachment_version (attachment_id: UUID; version_id: UUID): detachable ATTACHMENT
			-- 
			-- 
			-- 
			-- argument: attachment_id  (required)
			-- 
			-- argument: version_id  (required)
			-- 
			-- 
			-- Result ATTACHMENT
		require
		local
  			l_path: STRING
  			l_request: API_CLIENT_REQUEST
  			l_response: API_CLIENT_RESPONSE
		do
			reset_error
			create l_request
			
			l_path := "/api/v1/attachments/{attachment_id}/versions/{version_id}/restore"
			l_path.replace_substring_all ("{"+"attachment_id"+"}", api_client.url_encode (attachment_id.out))
			l_path.replace_substring_all ("{"+"version_id"+"}", api_client.url_encode (version_id.out))


			if attached {STRING} api_client.select_header_accept ({ARRAY [STRING]}<<"application/json">>)  as l_accept then
				l_request.add_header(l_accept,"Accept");
			end
			l_request.add_header(api_client.select_header_content_type ({ARRAY [STRING]}<<>>),"Content-Type")
			l_request.set_auth_names ({ARRAY [STRING]}<<"bearer_token">>)
			l_response := api_client.call_api (l_path, "Post", l_request, Void, agent deserializer)
			if l_response.has_error then
				last_error := l_response.error
			elseif attached { ATTACHMENT } l_response.data ({ ATTACHMENT }) as l_data then
				Result := l_data
			else
				create last_error.make ("Unknown error: Status response [ " + l_response.status.out + "]")
			end
		end


end
