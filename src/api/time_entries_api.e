note
 description:"[
		SimpleBilly API
 		Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
  		The version of the OpenAPI document: 0.1.0
 	    Contact: support@simplebilly.com

  	NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).

 		 Do not edit the class manually.
 	]"
	date: "$Date$"
	revision: "$Revision$"
	EIS:"Eiffel openapi generator", "src=https://openapi-generator.tech", "protocol=uri"

class
	TIMEENTRIES_API

inherit

    API_I


feature -- API Access


	clock_in_time_entry (time_entry_clock_in: TIME_ENTRY_CLOCK_IN): detachable TIME_ENTRY_DTO
			-- Clock in for the authenticated user (resolved via their employee profile).
			-- 
			-- 
			-- argument: time_entry_clock_in  (required)
			-- 
			-- 
			-- Result TIME_ENTRY_DTO
		require
		local
  			l_path: STRING
  			l_request: API_CLIENT_REQUEST
  			l_response: API_CLIENT_RESPONSE
		do
			reset_error
			create l_request
			l_request.set_body(time_entry_clock_in)
			l_path := "/api/v1/time-entries"


			if attached {STRING} api_client.select_header_accept ({ARRAY [STRING]}<<"application/json">>)  as l_accept then
				l_request.add_header(l_accept,"Accept");
			end
			l_request.add_header(api_client.select_header_content_type ({ARRAY [STRING]}<<"application/json">>),"Content-Type")
			l_request.set_auth_names ({ARRAY [STRING]}<<"bearer_token">>)
			l_response := api_client.call_api (l_path, "Post", l_request, Void, agent deserializer)
			if l_response.has_error then
				last_error := l_response.error
			elseif attached { TIME_ENTRY_DTO } l_response.data ({ TIME_ENTRY_DTO }) as l_data then
				Result := l_data
			else
				create last_error.make ("Unknown error: Status response [ " + l_response.status.out + "]")
			end
		end

	clock_out_time_entry (id: UUID; time_entry_clock_out: TIME_ENTRY_CLOCK_OUT): detachable TIME_ENTRY_DTO
			-- Clock out an entry: the entry&#39;s owner, or anyone with &#x60;time_entries:write&#x60;.
			-- 
			-- 
			-- argument: id  (required)
			-- 
			-- argument: time_entry_clock_out  (required)
			-- 
			-- 
			-- Result TIME_ENTRY_DTO
		require
		local
  			l_path: STRING
  			l_request: API_CLIENT_REQUEST
  			l_response: API_CLIENT_RESPONSE
		do
			reset_error
			create l_request
			l_request.set_body(time_entry_clock_out)
			l_path := "/api/v1/time-entries/{id}"
			l_path.replace_substring_all ("{"+"id"+"}", api_client.url_encode (id.out))


			if attached {STRING} api_client.select_header_accept ({ARRAY [STRING]}<<"application/json">>)  as l_accept then
				l_request.add_header(l_accept,"Accept");
			end
			l_request.add_header(api_client.select_header_content_type ({ARRAY [STRING]}<<"application/json">>),"Content-Type")
			l_request.set_auth_names ({ARRAY [STRING]}<<"bearer_token">>)
			l_response := api_client.call_api (l_path, "Patch", l_request, Void, agent deserializer)
			if l_response.has_error then
				last_error := l_response.error
			elseif attached { TIME_ENTRY_DTO } l_response.data ({ TIME_ENTRY_DTO }) as l_data then
				Result := l_data
			else
				create last_error.make ("Unknown error: Status response [ " + l_response.status.out + "]")
			end
		end

	labor_costs (var_from: DATE; to: DATE; group_by: STRING_32): detachable LIST [LABOR_COST_ROW]
			-- Labor-cost report: worked hours aggregated per employee / order / day, valued at the employee&#39;s hourly cost rate.
			-- 
			-- 
			-- argument: var_from  (required)
			-- 
			-- argument: to  (required)
			-- 
			-- argument: group_by One of \&quot;employee\&quot;, \&quot;order\&quot; or \&quot;day\&quot;. (required)
			-- 
			-- 
			-- Result LIST [LABOR_COST_ROW]
		require
		local
  			l_path: STRING
  			l_request: API_CLIENT_REQUEST
  			l_response: API_CLIENT_RESPONSE
		do
			reset_error
			create l_request
			
			l_path := "/api/v1/labor-costs"
			l_request.fill_query_params(api_client.parameter_to_tuple("", "from", var_from));
			l_request.fill_query_params(api_client.parameter_to_tuple("", "to", to));
			l_request.fill_query_params(api_client.parameter_to_tuple("", "group_by", group_by));


			if attached {STRING} api_client.select_header_accept ({ARRAY [STRING]}<<"application/json">>)  as l_accept then
				l_request.add_header(l_accept,"Accept");
			end
			l_request.add_header(api_client.select_header_content_type ({ARRAY [STRING]}<<>>),"Content-Type")
			l_request.set_auth_names ({ARRAY [STRING]}<<"bearer_token">>)
			l_response := api_client.call_api (l_path, "Get", l_request, Void, agent deserializer)
			if l_response.has_error then
				last_error := l_response.error
			elseif attached { LIST [LABOR_COST_ROW] } l_response.data ({ LIST [LABOR_COST_ROW] }) as l_data then
				Result := l_data
			else
				create last_error.make ("Unknown error: Status response [ " + l_response.status.out + "]")
			end
		end

	list_time_entries (var_from: DATE; to: DATE; active: BOOLEAN; employee_id: UUID): detachable LIST [TIME_ENTRY_DTO]
			-- List time entries with optional date-range / active / employee filters.
			-- 
			-- 
			-- argument: var_from  (optional, default to null)
			-- 
			-- argument: to  (optional, default to null)
			-- 
			-- argument: active Only currently running shifts (clock_in set, clock_out null). (optional, default to null)
			-- 
			-- argument: employee_id  (optional, default to null)
			-- 
			-- 
			-- Result LIST [TIME_ENTRY_DTO]
		require
		local
  			l_path: STRING
  			l_request: API_CLIENT_REQUEST
  			l_response: API_CLIENT_RESPONSE
		do
			reset_error
			create l_request
			
			l_path := "/api/v1/time-entries"
			l_request.fill_query_params(api_client.parameter_to_tuple("", "from", var_from));
			l_request.fill_query_params(api_client.parameter_to_tuple("", "to", to));
			l_request.fill_query_params(api_client.parameter_to_tuple("", "active", active));
			l_request.fill_query_params(api_client.parameter_to_tuple("", "employee_id", employee_id));


			if attached {STRING} api_client.select_header_accept ({ARRAY [STRING]}<<"application/json">>)  as l_accept then
				l_request.add_header(l_accept,"Accept");
			end
			l_request.add_header(api_client.select_header_content_type ({ARRAY [STRING]}<<>>),"Content-Type")
			l_request.set_auth_names ({ARRAY [STRING]}<<"bearer_token">>)
			l_response := api_client.call_api (l_path, "Get", l_request, Void, agent deserializer)
			if l_response.has_error then
				last_error := l_response.error
			elseif attached { LIST [TIME_ENTRY_DTO] } l_response.data ({ LIST [TIME_ENTRY_DTO] }) as l_data then
				Result := l_data
			else
				create last_error.make ("Unknown error: Status response [ " + l_response.status.out + "]")
			end
		end


end
