note
 description:"[
		SimpleBilly API
 		Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
  		The version of the OpenAPI document: 0.1.0
 	    Contact: support@simplebilly.com

  	NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).

 		 Do not edit the class manually.
 	]"
	date: "$Date$"
	revision: "$Revision$"
	EIS:"Eiffel openapi generator", "src=https://openapi-generator.tech", "protocol=uri"

class
	CUSTOMERCOMMUNICATION_API

inherit

    API_I


feature -- API Access


	communication (communication_id: STRING_32): detachable CUSTOMER_COMMUNICATION
			-- 
			-- 
			-- 
			-- argument: communication_id  (required)
			-- 
			-- 
			-- Result CUSTOMER_COMMUNICATION
		require
		local
  			l_path: STRING
  			l_request: API_CLIENT_REQUEST
  			l_response: API_CLIENT_RESPONSE
		do
			reset_error
			create l_request
			
			l_path := "/api/v1/communications/{communication_id}"
			l_path.replace_substring_all ("{"+"communication_id"+"}", api_client.url_encode (communication_id.out))


			if attached {STRING} api_client.select_header_accept ({ARRAY [STRING]}<<"application/json">>)  as l_accept then
				l_request.add_header(l_accept,"Accept");
			end
			l_request.add_header(api_client.select_header_content_type ({ARRAY [STRING]}<<>>),"Content-Type")
			l_request.set_auth_names ({ARRAY [STRING]}<<"bearer_token">>)
			l_response := api_client.call_api (l_path, "Get", l_request, Void, agent deserializer)
			if l_response.has_error then
				last_error := l_response.error
			elseif attached { CUSTOMER_COMMUNICATION } l_response.data ({ CUSTOMER_COMMUNICATION }) as l_data then
				Result := l_data
			else
				create last_error.make ("Unknown error: Status response [ " + l_response.status.out + "]")
			end
		end

	contact_history (contact_id: STRING_32): detachable CONTACT_HISTORY_RESPONSE
			-- 
			-- 
			-- 
			-- argument: contact_id  (required)
			-- 
			-- 
			-- Result CONTACT_HISTORY_RESPONSE
		require
		local
  			l_path: STRING
  			l_request: API_CLIENT_REQUEST
  			l_response: API_CLIENT_RESPONSE
		do
			reset_error
			create l_request
			
			l_path := "/api/v1/contacts/{contact_id}/communications"
			l_path.replace_substring_all ("{"+"contact_id"+"}", api_client.url_encode (contact_id.out))


			if attached {STRING} api_client.select_header_accept ({ARRAY [STRING]}<<"application/json">>)  as l_accept then
				l_request.add_header(l_accept,"Accept");
			end
			l_request.add_header(api_client.select_header_content_type ({ARRAY [STRING]}<<>>),"Content-Type")
			l_request.set_auth_names ({ARRAY [STRING]}<<"bearer_token">>)
			l_response := api_client.call_api (l_path, "Get", l_request, Void, agent deserializer)
			if l_response.has_error then
				last_error := l_response.error
			elseif attached { CONTACT_HISTORY_RESPONSE } l_response.data ({ CONTACT_HISTORY_RESPONSE }) as l_data then
				Result := l_data
			else
				create last_error.make ("Unknown error: Status response [ " + l_response.status.out + "]")
			end
		end

	create_communication (customer_communication_create: CUSTOMER_COMMUNICATION_CREATE): detachable CUSTOMER_COMMUNICATION
			-- 
			-- 
			-- 
			-- argument: customer_communication_create  (required)
			-- 
			-- 
			-- Result CUSTOMER_COMMUNICATION
		require
		local
  			l_path: STRING
  			l_request: API_CLIENT_REQUEST
  			l_response: API_CLIENT_RESPONSE
		do
			reset_error
			create l_request
			l_request.set_body(customer_communication_create)
			l_path := "/api/v1/communications"


			if attached {STRING} api_client.select_header_accept ({ARRAY [STRING]}<<"application/json">>)  as l_accept then
				l_request.add_header(l_accept,"Accept");
			end
			l_request.add_header(api_client.select_header_content_type ({ARRAY [STRING]}<<"application/json">>),"Content-Type")
			l_request.set_auth_names ({ARRAY [STRING]}<<"bearer_token">>)
			l_response := api_client.call_api (l_path, "Post", l_request, Void, agent deserializer)
			if l_response.has_error then
				last_error := l_response.error
			elseif attached { CUSTOMER_COMMUNICATION } l_response.data ({ CUSTOMER_COMMUNICATION }) as l_data then
				Result := l_data
			else
				create last_error.make ("Unknown error: Status response [ " + l_response.status.out + "]")
			end
		end

	customercommunication_restore (communication_id: STRING_32): detachable CUSTOMER_COMMUNICATION
			-- 
			-- 
			-- 
			-- argument: communication_id  (required)
			-- 
			-- 
			-- Result CUSTOMER_COMMUNICATION
		require
		local
  			l_path: STRING
  			l_request: API_CLIENT_REQUEST
  			l_response: API_CLIENT_RESPONSE
		do
			reset_error
			create l_request
			
			l_path := "/api/v1/communications/{communication_id}/restore"
			l_path.replace_substring_all ("{"+"communication_id"+"}", api_client.url_encode (communication_id.out))


			if attached {STRING} api_client.select_header_accept ({ARRAY [STRING]}<<"application/json">>)  as l_accept then
				l_request.add_header(l_accept,"Accept");
			end
			l_request.add_header(api_client.select_header_content_type ({ARRAY [STRING]}<<>>),"Content-Type")
			l_request.set_auth_names ({ARRAY [STRING]}<<"bearer_token">>)
			l_response := api_client.call_api (l_path, "Post", l_request, Void, agent deserializer)
			if l_response.has_error then
				last_error := l_response.error
			elseif attached { CUSTOMER_COMMUNICATION } l_response.data ({ CUSTOMER_COMMUNICATION }) as l_data then
				Result := l_data
			else
				create last_error.make ("Unknown error: Status response [ " + l_response.status.out + "]")
			end
		end

	delete_communication (communication_id: STRING_32)
			-- 
			-- 
			-- 
			-- argument: communication_id  (required)
			-- 
			-- 
		require
		local
  			l_path: STRING
  			l_request: API_CLIENT_REQUEST
  			l_response: API_CLIENT_RESPONSE
		do
			reset_error
			create l_request
			
			l_path := "/api/v1/communications/{communication_id}"
			l_path.replace_substring_all ("{"+"communication_id"+"}", api_client.url_encode (communication_id.out))


			if attached {STRING} api_client.select_header_accept ({ARRAY [STRING]}<<"application/json">>)  as l_accept then
				l_request.add_header(l_accept,"Accept");
			end
			l_request.add_header(api_client.select_header_content_type ({ARRAY [STRING]}<<>>),"Content-Type")
			l_request.set_auth_names ({ARRAY [STRING]}<<"bearer_token">>)
			l_response := api_client.call_api (l_path, "Delete", l_request, agent serializer, Void)
			if l_response.has_error then
				last_error := l_response.error
			end
		end

	list_communications (page: INTEGER_32; page_size: INTEGER_32; contact_id: STRING_32; channel: detachable COMMUNICATION_CHANNEL; direction: detachable COMMUNICATION_DIRECTION; var_from: DATE; to: DATE): detachable LIST [CUSTOMER_COMMUNICATION]
			-- 
			-- 
			-- 
			-- argument: page  (optional, default to null)
			-- 
			-- argument: page_size  (optional, default to null)
			-- 
			-- argument: contact_id Filter history to a single contact. (optional, default to null)
			-- 
			-- argument: channel  (optional, default to null)
			-- 
			-- argument: direction  (optional, default to null)
			-- 
			-- argument: var_from Only include communications after this ISO date (inclusive). (optional, default to null)
			-- 
			-- argument: to Only include communications before this ISO date (inclusive). (optional, default to null)
			-- 
			-- 
			-- Result LIST [CUSTOMER_COMMUNICATION]
		require
     		page_is_greater_or_equal_than: page >= 0
     		page_size_is_greater_or_equal_than: page_size >= 0
		local
  			l_path: STRING
  			l_request: API_CLIENT_REQUEST
  			l_response: API_CLIENT_RESPONSE
		do
			reset_error
			create l_request
			
			l_path := "/api/v1/communications/"
			l_request.fill_query_params(api_client.parameter_to_tuple("", "page", page));
			l_request.fill_query_params(api_client.parameter_to_tuple("", "page_size", page_size));
			l_request.fill_query_params(api_client.parameter_to_tuple("", "contact_id", contact_id));
			l_request.fill_query_params(api_client.parameter_to_tuple("", "channel", channel));
			l_request.fill_query_params(api_client.parameter_to_tuple("", "direction", direction));
			l_request.fill_query_params(api_client.parameter_to_tuple("", "from", var_from));
			l_request.fill_query_params(api_client.parameter_to_tuple("", "to", to));


			if attached {STRING} api_client.select_header_accept ({ARRAY [STRING]}<<"application/json">>)  as l_accept then
				l_request.add_header(l_accept,"Accept");
			end
			l_request.add_header(api_client.select_header_content_type ({ARRAY [STRING]}<<>>),"Content-Type")
			l_request.set_auth_names ({ARRAY [STRING]}<<"bearer_token">>)
			l_response := api_client.call_api (l_path, "Get", l_request, Void, agent deserializer)
			if l_response.has_error then
				last_error := l_response.error
			elseif attached { LIST [CUSTOMER_COMMUNICATION] } l_response.data ({ LIST [CUSTOMER_COMMUNICATION] }) as l_data then
				Result := l_data
			else
				create last_error.make ("Unknown error: Status response [ " + l_response.status.out + "]")
			end
		end

	update_communication (communication_id: STRING_32; customer_communication_update: CUSTOMER_COMMUNICATION_UPDATE): detachable CUSTOMER_COMMUNICATION
			-- 
			-- 
			-- 
			-- argument: communication_id  (required)
			-- 
			-- argument: customer_communication_update  (required)
			-- 
			-- 
			-- Result CUSTOMER_COMMUNICATION
		require
		local
  			l_path: STRING
  			l_request: API_CLIENT_REQUEST
  			l_response: API_CLIENT_RESPONSE
		do
			reset_error
			create l_request
			l_request.set_body(customer_communication_update)
			l_path := "/api/v1/communications/{communication_id}"
			l_path.replace_substring_all ("{"+"communication_id"+"}", api_client.url_encode (communication_id.out))


			if attached {STRING} api_client.select_header_accept ({ARRAY [STRING]}<<"application/json">>)  as l_accept then
				l_request.add_header(l_accept,"Accept");
			end
			l_request.add_header(api_client.select_header_content_type ({ARRAY [STRING]}<<"application/json">>),"Content-Type")
			l_request.set_auth_names ({ARRAY [STRING]}<<"bearer_token">>)
			l_response := api_client.call_api (l_path, "Put", l_request, Void, agent deserializer)
			if l_response.has_error then
				last_error := l_response.error
			elseif attached { CUSTOMER_COMMUNICATION } l_response.data ({ CUSTOMER_COMMUNICATION }) as l_data then
				Result := l_data
			else
				create last_error.make ("Unknown error: Status response [ " + l_response.status.out + "]")
			end
		end


end
